import React from "react";
import Layout from "./Layout";

export default {
  title: "Layout/Layout",
  component: Layout,
};

const Template = (args: any) => <Layout {...args} />;
export const Default = Template.bind({});
